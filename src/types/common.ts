export type main = {
    name: string
    path: string
}

// you can use everywhere that type with expport
export interface Traveltypes {
    id: string
    title: string
    image: string
    startDate: string
    endDate: string
    menus: string
    amount: string
    flightAmount?: string
}